<UserControl x:Name="DetailsControls" x:Class="WpfMaze.DetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfMaze"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <TextBlock Padding="3" Margin="3">Maze Name:</TextBlock>
        <TextBox x:Name="txtName" Grid.Column="2" Text="{Binding VM_MazeName}" Padding="3" Margin="3"/>
        <TextBlock Grid.Row="1" Margin="3">Maze rows:</TextBlock>
        <TextBox x:Name="txtRows" Grid.Row="1" Grid.Column="2" Padding="3" Text="{Binding VM_MazeRows}"  Margin="3" />

        <TextBlock Grid.Row="2" Padding="3" Margin="3" >Maze Columns:</TextBlock>
        <TextBox x:Name="txtCols" Grid.Row="2" Grid.Column="2" Text="{Binding VM_MazeCols}" Padding="3" Margin="3" />

    </Grid>
</UserControl>
